<%= form_for [:admin, @booking] do |f| %>
  <%= render "shared/error_messages", object: f.object %>
  <%= f.label :user %>
  <%= f.select :user_id, select_user_id, prompt: t(".select"),
                              class: "form-control" %>
  <div class="row hotel_booking_table">
    <div class="col-md-3 table-title">
      <h2><%= t ".book" %><br> <%= t ".your_room" %></h2>
    </div>
    <div class="col-md-9">
      <div class="boking_table">
        <div class="row">
          <div class="col-md-4">
            <div class="book_tabel_item">
              <div class="form-group">
                <div class="input-group date">
                  <%= f.date_field :checkin, placeholder: t(".arrival"), class: "form-control" %>
                  <span class="input-group-addon">
                    <span class="glyphicon glyphicon-calendar"></span>
                  </span>
                </div>
              </div>
              <div class="form-group">
                <div class="input-group date" id="datetimepicker">
                  <%= f.date_field :checkout, placeholder: t(".departure"), class: "form-control" %>
                  <span class="input-group-addon">
                    <span class="glyphicon glyphicon-calendar"></span>
                  </span>
                </div>
              </div>
            </div>
          </div>
          <div class="col-md-4">
            <div class="book_tabel_item">
              <div class="input-group">
                <%= f.collection_select :room_type, @room_types, :id, :name,
                  {prompt: t(".room_type")}, required: true,
                  class: "nice-select" %>
              </div>
              <div class="input-group">
                <%= f.select :total, [], {prompt: t(".number")}, required: true,
                class: "nice-select" %>
              </div>
            </div>
          </div>
          <div class="col-md-4">
            <div class="book_tabel_item">
            <%= f.submit t(".book_now"), class: "book_now_btn button_hover" %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>
